# طقس الطيران — مساعد الطيارين لمطار البحرين الدولي (OBBI)

موقع ويب يعرض بيانات METAR وTAF الحية لمطار البحرين الدولي (ويدعم أي مطار آخر عبر رمز ICAO)، مع:

- شارة حالة الطيران الملوّنة (VFR / MVFR / IFR / LIFR)
- بوصلة رياح تفاعلية
- حاسبة الرياح الجانبية والأمامية لمدرجي OBBI (استناداً إلى اتجاه الرياح الحالي)
- تقرير METAR وTAF الخام + جدول تفاصيل مفكوك الترميز
- جدول زمني لتوقعات TAF مع تصنيف كل فترة
- فترة النهار (شروق/غروب/طول النهار) لمطار البحرين
- تحديث تلقائي كل 5 دقائق

## التشغيل

المصدر الرسمي لبيانات الطقس (`aviationweather.gov`) لا يسمح بطلبات المتصفح المباشرة (CORS)،
لذلك يتضمن المشروع خادم بايثون بسيط (بدون أي مكتبات خارجية) يخدم الملفات الثابتة
ويعمل كوسيط لجلب البيانات:

```bash
python server.py
```

ثم افتح المتصفح على: `http://localhost:8765`

يمكن تغيير المنفذ: `python server.py 9000`

## بنية المشروع

- `index.html` — الصفحة الرئيسية
- `style.css` — التنسيق
- `script.js` — منطق جلب البيانات، الحسابات، والعرض
- `navaids.js` — جداول الوسائل الملاحية القريبة من OBBI
- `server.py` — خادم محلي للتطوير (الملفات الثابتة + وسيط CORS)
- `functions/proxy/[kind].js` — نفس الوسيط، كدالة Cloudflare Pages Function للنشر الفعلي

## النشر على الإنترنت (Cloudflare Pages)

الموقع جاهز للنشر مجاناً على Cloudflare Pages (بدون بطاقة ائتمان):

1. ارفع المجلد إلى مستودع GitHub.
2. من لوحة Cloudflare: **Workers & Pages → Create → Pages → Connect to Git**، واختر المستودع.
3. إعدادات البناء: Framework preset = None، Build command = فارغ، Build output directory = `/`.
4. بعد النشر ستحصل على رابط عام مثل `https://obbi-weather-site.pages.dev`.
5. (اختياري) لإضافة نطاق مخصص: اشترِ نطاقاً من أي مسجّل ثم أضفه من إعدادات المشروع → Custom domains (مجاني الإضافة، تكلفة النطاق فقط).

`server.py` يبقى مفيداً للتطوير والاختبار المحلي فقط؛ عند النشر على Cloudflare تُستخدم دالة
`functions/proxy/[kind].js` تلقائياً بدلاً منه (نفس المنطق، بلغة JavaScript).

## ملاحظة مهمة

هذه الأداة لأغراض معلوماتية ومساعدة عامة فقط، ولا تُغني عن مصادر الطقس والنوتام
(NOTAM) الرسمية المعتمدة عند التخطيط الفعلي للرحلات.
